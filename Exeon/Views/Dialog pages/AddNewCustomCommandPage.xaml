<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Exeon.Views.Dialog_pages.AddNewCustomCommandPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Exeon.Views.Dialog_pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!--Trigger commands ListBox data template-->
        <DataTemplate x:Key="TCItemTemplate">
            <Grid>
                <Border
                    HorizontalAlignment="Left"
                    CornerRadius="5"
                    Width="auto"
                    Background="{ThemeResource AccentFillColorSecondaryBrush}">

                    <StackPanel 
                        Margin="10,0,0,0"
                        Height="auto"
                        Orientation="Horizontal"
                        Spacing="10">

                        <TextBlock
                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                            Margin="0,4,0,0"
                            Text="{Binding CommandText}"/>

                        <Button x:Name="DeleteTrigger" Click="DeleteTrigger_Click" Style="{StaticResource DeleteTriggerCommandButtonStyle}" Width="30" Height="30">
                            <Viewbox Margin="-3.5">
                                <SymbolIcon
                                    Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                    Symbol="Cancel"/>
                            </Viewbox>
                        </Button>

                    </StackPanel>
                </Border>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <TextBox
            x:Name="EnterTriggerCommandTextBox"
            Grid.Column="0"
            PlaceholderText="Введіть команду тригер..."/>

        <Button
            x:Name="AddNewTriggerCommandBtn"
            Grid.Column="1"
            Margin="15,0,0,0"
            Height="30"
            Width="30"
            Style="{ThemeResource AccentButtonStyle}"
            Click="AddNewTriggerCommandBtn_Click">

            <Viewbox Margin="-5">
                <SymbolIcon
                    Symbol="Add"/>
            </Viewbox>
        </Button>

        <InfoBar
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            x:Name="ErrorInfoBar"
            Margin="0,10,0,0"
            Severity="Error"/>

        <ListView
            x:Name="TriggerCommandsList"
            Margin="0,15,0,0"
            ItemTemplate="{StaticResource TCItemTemplate}"
            ItemsSource="{x:Bind ViewModel.TemporaryTriggerCommandsCollection}"
            SelectionMode="None"
            MaxHeight="100"
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="2">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</Page>
